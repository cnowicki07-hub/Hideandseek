{
  "name": "hide-and-seek",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node test/e2e.mjs",
    "quota": "node test/quota.mjs",
    "vendor:agents": "esbuild node_modules/agents/client.js --bundle --format=iife --global-name=AgentsClient --outfile=public/vendor/agents-client.js --minify"
  },
  "dependencies": {
    "agents": "^0.22.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "playwright": "^1.50.0",
    "wrangler": "^4.0.0"
  }
}
